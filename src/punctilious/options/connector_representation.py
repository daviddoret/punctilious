import punctilious.pu_03_representation as _representation

symbol = _representation.Option('connector_representation', 'symbol')
"""The connector is represented as a symbol. E.g.: `∧` for the logical conjunction operator."""

word = _representation.Option('connector_representation', 'word')
"""The connector is represented as a symbol. E.g.: `and` for the logical conjunction operator."""

pass
