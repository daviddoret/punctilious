schema: http://punctilious.org/data/schemas/punctilious_package_schema_1.yaml
uuid4: 54dedacd-6701-45ce-ba2c-17ab1997f85b # assure the unicity of the package.
slug: package_test_1
imports: # list of dependent files
  # to reference elements in a dependent file, use the dot separator, e.g.:
  # test_file_2.some_element_1
  - slug: source_1
    source: http://punctilious.org/data/test/external_source_1.yaml
    method: slugs | dotted_names
aliases:
# - slug_1: source_1.external_object_1
# - slug_2: source_1.external_object_2
# - slug_3: source_1.external_object_3
representations:
  # symbols are reusable connector presentations.
  # practical for variable names, etc.
  - slug: variable_p
    uuid4: 4d8f91cf-67e9-4f6b-b367-a3d3a5bcf95e # assure the unicity of the symbol.
    syntactic_rules:
      fixed_arity: 0
    configurations:
      - mode: symbolic # neutral, symbolic, textual
        encoding: unicode_basic # unicode_basic, unicode_extended, latex_math, mathml, ...
        template: "P"
      - mode: symbolic
        encoding: unicode_extended
        template: "𝑃"
      - mode: symbolic
        encoding: latex_math
        template: "\\textit{P}"
  - slug: conjunction_formula
    uuid4: e0b65eef-9186-4c6b-97b3-ccf52df23450 # assure the unicity of the connector.
    syntactic_rules:
      fixed_arity: 2
      # min_arity
      # max_arity
      # ...
    configurations:
      - mode: symbolic # neutral, symbolic, textual
        # layout: neutral # neutral, inline, compact, expanded
        language: en # neutral, fr, fr_CH, ...
        encoding: unicode_basic # unicode_basic, unicode_extended, latex_math, mathml, ...
        template: "{{a1}} and {{a2}}"
      - mode: symbolic
        # layout: neutral
        # language: neutral
        encoding: unicode_extended
        template: "{{a1}} ∧ {{a2}}"
      - mode: symbolic
        layout: neutral
        language: neutral
        encoding: latex_math
        template: "{{a1}} \\land {{a2}}"
      - mode: symbolic
        layout: neutral
        language: fr
        encoding: unicode_basic
        template: "{{a1}} et {{a2}}"
connectors:
  - slug: p
    uuid4: 7a051cb9-b5f4-4b86-9db6-06f688d9ee95 # assure the unicity of the connector.
    syntactic_rules:
      fixed_arity: 0
    representation: variable_p
  - slug: conjunction
    uuid4: e8d2ffca-cf7e-4e62-9b5b-ce45b252b926 # assure the unicity of the connector.
    syntactic_rules:
      fixed_arity: 2
    representation: conjunction_formula
theorems:
derivations: