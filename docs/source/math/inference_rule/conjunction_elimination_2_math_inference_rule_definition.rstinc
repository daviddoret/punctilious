Definition
----------

*conjunction-elimination-2* is the :ref:`inference-rule<inference_rule_math_concept>`:

.. math::

   \left( \boldsymbol{P} \land \mathbf{Q} \right) \vdash \mathbf{Q}

Where:

   * :math:`\boldsymbol{P} \land \mathbf{Q}` is a :ref:`formula-statement<formula_statement_math_concept>`
   * :math:`\boldsymbol{P}` is a propositional :ref:`formula<formula_math_concept>`
   * :math:`\mathbf{Q}` is a propositional :ref:`formula<formula_math_concept>`

In straightforward language, if both :math:`\boldsymbol{P}` and :math:`\mathbf{Q}` are true, it follows that :math:`\mathbf{Q}` is true.

Sources
----------

* :cite:p:`warner_2018_puremathematicsbeginnersrigorous{p. 114}`
